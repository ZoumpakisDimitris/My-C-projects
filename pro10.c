#include <stdio.h>

int main(){
    int var_1=10; // declaring and initializing two variables
    int var_2=20;
    int sum=var_1+var_2; // calculating the sum of the two variables
        printf("Operation:  %d + %d = %d" , var_1,var_2, sum); // displaying the operation and the result

    
    return 0;
}